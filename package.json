{
	"name": "capthook",
	"version": "1.0.0",
	"description": "A CLS like library to keep track of your asynchronous contexts",
	"main": "dist/index.js",
	"license": "MIT",
	"devDependencies": {
		"@types/jest": "^24.0.13",
		"@types/node": "^12.0.2",
		"jest": "^24.8.0",
		"ts-jest": "^24.0.2",
		"ts-node": "^8.1.0",
		"typescript": "^3.4.5"
	},
	"scripts": {
		"test": "jest",
		"build": "rm -rf dist && tsc"
	},
	"engines": {
		"node": ">=10.0.0"
	}
}
